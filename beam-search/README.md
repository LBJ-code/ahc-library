# 差分更新ビームサーチのテンプレート

この記事内の C++ 実装を、用途ごとに分けて収録しています。

| ファイル | 記事内の実装 | 記事内での掲載・更新 |
| --- | --- | --- |
| `double-linked-object-pool.hpp` | 二重連鎖木 + `ObjectPool` の通常版 | 2024-02-01 の本文 |
| `multi-turn.hpp` | 1つの `Action` が複数ターン先へ遷移できる版（ターンが飛ぶ版） | 2025-03-16 追記、2025-07-29 バグ修正追記 |
| `euler-tour-edges.hpp` | 二重連鎖木や `ObjectPool` を使わず Euler Tour の有向辺を配列で保持する版 | 2024-02-07 追記 |

## 出典と利用許可

- 原著者: `eijirou_kyopro` (eijirou)
- 記事・原典 URL: <https://eijirou-kyopro.hatenablog.com/entry/2024/02/01/115639>
- 取得日: 2026-08-29 (JST)
- 記事の利用条件: 著者は記事本文で「この記事に書いたソースコードをプログラミングコンテストで自由に使っていただいて構いません」と明記しています。
- SPDX 識別子を持つ標準ライセンスの記載は記事・原典にありません。したがって、MIT/BSD 等のライセンス名を付していません。利用条件の根拠と免責は [NOTICE.md](NOTICE.md) に残しています。

記事が参考文献として挙げる [「高速なビームサーチが欲しい!!!」 (Qiita)](https://qiita.com/rhoo/items/f2be256cde5ad2e62dde) も確認しました。これは差分更新の考え方を解説する別記事で、今回の3ファイルの C++ 原典ではありません。確認したページには独立したコード再配布ライセンスの記載が見当たらないため、Qiita側のコードは収録していません。記事本文にも別のソースコードリポジトリへのリンクはなく、3つの原典は上記 Hatena 記事内のコードブロックです。

## 変更内容と検証

コード本体は記事の各コードブロックから抽出し、HTML のシンタックスハイライト用タグを除去しただけです。アルゴリズム、`TODO`、コメントを含むコード本体は改変していません。各ファイル先頭の出典メタデータは、このリポジトリへの収録のために追加しています。

これらは問題固有の `Action`、`Evaluator`、`State` を `TODO` としたテンプレートです。そのため単独で完成したプログラムとして動作するものではなく、記事と同様に問題へ組み込む実装例です。特に前者2つは AtCoder Library (`<atcoder/all>` または `<atcoder/segtree>`) に依存します。`TODO` を埋めた利用側コードで、対象問題・環境に合わせてコンパイル検証してください。

収録対象の特定、記事本文の利用条件、コードブロックとの照合は 2026-08-29 に実施しました。3ファイルを同一翻訳単位へ同時に include する用途ではなく、必要な実装を1つ選んで利用してください（いずれも `beam_search` 名前空間内に同名型を定義します）。

### 抽出照合値

下記は、各ファイルの出典メタデータを除いたコード本体（記事 HTML のタグを除去した UTF-8 テキスト、末尾改行を含む）の値です。これにより、3ブロックの取り違えや意図しない本文変更を確認できます。

| ファイル | コード本体の行数 | コード本体 SHA-256 |
| --- | ---: | --- |
| `double-linked-object-pool.hpp` | 525 | `20bdaee6b860dc859b80626fb30693a28f9247f334d910aef1852abcf1d7724c` |
| `multi-turn.hpp` | 637 | `f126841f8d22cba7277f839731c0029a4571d36ef2c5a958e1c23b6359ff6f9a` |
| `euler-tour-edges.hpp` | 459 | `4fc1232e71e21dc09cfc6050127c51a67a6dd96e8ba7f133c937261c643f25d3` |

### コンパイル確認

2026-08-29 に公式 `ac-library` を一時取得し、`g++ 13.3.0 -std=c++23 -fsyntax-only` で3ファイルを確認しました。いずれも構文エラーはありません。原典の `TODO` にある未実装の戻り値については、想定どおり警告が出ます。

`euler-tour-edges.hpp` は原典どおり `atcoder::segtree` を使用しますが、`<atcoder/segtree>` を自分では include していません。利用側で先に include した条件で確認しています。これらは未実装の問題依存部を含むテンプレートであり、TODOを埋めずに実行できる完成済みライブラリではありません。
